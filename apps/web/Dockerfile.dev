FROM node:20-alpine

WORKDIR /app

# Preinstall frontend dependencies for faster first boot.
COPY apps/web/package*.json ./apps/web/
RUN npm --prefix apps/web install

EXPOSE 3000

CMD ["npm", "run", "--prefix", "apps/web", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
